You are an expert technical reviewer doing an independent, adversarial DOCS review of the Tensotron library before it is announced publicly (it will be posted to the ILGPU Discord). Your job is to catch any claim that is wrong, overstated, unreproducible, or out of sync with the code — before strangers do.

Context: Tensotron is a float32-only, PyTorch-faithful tensor + autograd library for .NET 8, built on ILGPU (CUDA) with a managed/SIMD CPU backend. Read README.md and CLAUDE.md first.

Scope of THIS review = the DOCS and public-facing text only:
- README.md (claims, perf numbers, scope list, quick-start code, backend descriptions, installation)
- docs/PERFORMANCE_VS_PYTORCH.md (methodology, tables, the "why")
- PACKAGE.md (the packed NuGet readme)
- docs/ (any other markdown) and docs/artifacts/ as relevant
- Any inline code samples in the docs (do they match the CURRENT public API surface in src/Tensotron?)

Hunt specifically for:
1. Accuracy vs code: every API name, signature, env var (TENSOTRON_BACKEND, TENSOTRON_CPU_THREADS, etc.), and code sample in the docs — does it actually exist and compile against src/Tensotron as written? Flag drift.
2. Performance claims: every number (8x CPU inference, ~6-8x capture, par-with-PyTorch GEMM, ~2-3x slower conv, 645x vs ILGPU scalar CPU, etc.) — is it backed by a reproducible benchmark in the repo (tools/bench, examples)? Is the methodology stated and fair? Flag any number that is asserted but not reproducible, or that reads as cherry-picked without the caveat.
3. Scope honesty: the "Not implemented" list (RNN/LSTM/GRU, attention, Embedding, ConvTranspose, Conv1d/3d, dtypes, state_dict/safetensors). Is it accurate and complete, or does something claimed-present actually missing, or something listed-absent actually present?
4. Overstatement / misleading framing for a public audience: anything that a skeptical ILGPU/ML expert would call hype, or that violates the project's own "state what IS, not history/lineage" doc rule.
5. Correctness of the PyTorch-parity claim and the quick-start example's stated outputs (e.g. x.Grad == [2,4,6]).
6. Broken links, stale paths, wrong commands, typos in commands.

Method: read the docs, then cross-check against the actual source and against what benchmarks actually exist in the repo. Cite file:line. Distinguish "verified wrong" from "couldn't verify". Rank by how badly it would embarrass the project publicly.

Write your final report (and ONLY the report) to scratch/audit_codexD_report.md with sections: ## Must-fix before announcing, ## Claims not reproducible / unverifiable, ## API/code-sample drift, ## Scope accuracy, ## Overstatement/framing, ## Minor (typos/links), ## Accurate & well-supported (brief). Each finding: file:line, the claim, why it's a problem, suggested correction. Do not print the report to stdout — write it to the file.
